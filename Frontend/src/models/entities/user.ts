export interface User {
    id: string;
    uuid: string;
    genre: string;
    nom: string;
    prenom: string;
    email: string;
    password: string;
    telephone: string;
    dateNaissance: Date;
}