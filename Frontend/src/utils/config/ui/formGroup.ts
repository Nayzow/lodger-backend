export const advancedSearchFormGroupUI = {
    label: {
        base: 'text-lg md:text-xl lg:text-2xl w-full  text-secondaire font-semibold font-ninetea',
        wrapper: 'flex items-center justify-between w-full'
    },
    container: 'flex flex-col gap-2',
    wrapper: 'flex flex-col gap-2',
}

export const basicSearchFormGroupUI = {
    wrapper: 'flex flex-col gap-2 p-4 text-lg w-fit',
    label: {
        base: 'font-bold text-secondaire text-lg',
    },
    container: 'flex flex-col gap-2',
}